# AI XLSX 处理系统 — 环境配置指南

> 适用平台：Windows 10/11 本地开发机
> 目标读者：工程造价人员 / AI 应用开发者
> 版本：v1.0 | 2026-05-07

---

## 1. 文档概述

本指南为在 Windows 环境下搭建 AI XLSX 处理系统提供完整的环境配置参考。内容涵盖技术选型决策、系统环境要求、搭建步骤、Anthropic xlsx Skill 适配方案以及常见问题排障。

**核心选型**：xlwings 为唯一主力库，openpyxl 仅在万行级以上纯数据批量读写场景作为可选加速方案。

---

## 2. 技术选型：为什么选 xlwings

### 2.1 核心架构差异

| 维度 | xlwings | openpyxl |
|------|---------|----------|
| **底层机制** | COM 接口调用 Excel 应用程序 | 纯 Python 解析 .xlsx XML 文件 |
| **运行依赖** | 需安装 Microsoft Excel | 无外部依赖 |
| **公式计算** | Excel 原生引擎，实时计算 | 不计算，需 LibreOffice 辅助 |
| **VBA 宏** | 完整支持调用和执行 | 不支持 |
| **文件格式** | .xls / .xlsx / .xlsm / .xlsb | .xlsx / .xlsm（不支持 .xls） |
| **模板保真** | 通过 Excel 操作，100% 保真 | 复杂格式/图表可能丢失 |

### 2.2 工程造价场景 8 维度对比

| 维度 | xlwings | openpyxl | 权重 | 胜出 |
|------|---------|----------|------|------|
| **公式重算** | ✅ Excel 原生引擎，实时计算 | ❌ 需额外安装 LibreOffice + recalc.py | ⭐⭐⭐⭐⭐ | xlwings |
| **VBA 宏支持** | ✅ 完整支持调用和执行 | ❌ 不支持 | ⭐⭐⭐⭐⭐ | xlwings |
| **模板保真度** | ✅ 通过 Excel 操作，100% 保真 | ⚠️ 复杂格式/图表可能丢失 | ⭐⭐⭐⭐⭐ | xlwings |
| **.xls 格式** | ✅ 支持 | ❌ 不支持 | ⭐⭐⭐⭐ | xlwings |
| **实时交互** | ✅ 可操作已打开的 Excel | ❌ 不能 | ⭐⭐⭐⭐ | xlwings |
| **批量数据处理** | ⚠️ 较慢（COM 交互开销） | ✅ 快 2-5 倍 | ⭐⭐⭐ | openpyxl |
| **跨平台** | ❌ 仅 Windows/macOS | ✅ 全平台 | ⭐⭐ | openpyxl |
| **服务器部署** | ❌ 需 Excel 桌面版 | ✅ 无依赖 | ⭐⭐ | openpyxl |

**高权重维度（⭐⭐⭐⭐ 以上）**：xlwings 5:0 全面领先。

### 2.3 选型决策规则

```
if xlwings批量数据处理速度 < openpyxl的1/3:
    批量数据处理场景提供 openpyxl 可选项
else:
    完全丢弃 openpyxl，仅用 xlwings
```

**实际基准数据**：xlwings 因 COM 交互开销，批量数据处理比 openpyxl 慢 2-5 倍，满足"慢三倍"条件。

### 2.4 最终选型结论

| 场景 | 推荐库 | 说明 |
|------|--------|------|
| 创建含公式/格式的报表 | **xlwings** | Excel 原生计算，零额外依赖 |
| 编辑现有工程造价模板 | **xlwings** | 100% 格式保真，VBA 宏可用 |
| 读取/分析 Excel 数据 | **xlwings** | 公式自动计算，结果可靠 |
| 公式验证/错误检测 | **xlwings** | Excel 引擎重算，无需 LibreOffice |
| 万行级纯数据批量读写 | **openpyxl**（可选） | 无公式/格式需求时，速度优势明显 |
| 数据分析/统计 | **pandas** | 配合 xlwings 或 openpyxl 读取数据 |

**何时考虑 openpyxl**：仅当同时满足以下条件时——
1. 数据量万行以上
2. 纯数据读写，无公式、无格式、无 VBA 需求
3. 处理速度是瓶颈

---

## 3. 系统环境要求（Windows）

### 3.1 硬件要求

| 项目 | 最低要求 | 推荐配置 |
|------|---------|---------|
| 操作系统 | Windows 10 (64-bit) | Windows 11 (64-bit) |
| 内存 | 8 GB | 16 GB+ |
| 磁盘空间 | 2 GB（Python + 依赖） | 5 GB+（含 Excel 项目文件） |

### 3.2 软件要求

| 软件 | 版本要求 | 用途 | 是否必须 |
|------|---------|------|---------|
| **Microsoft Excel** | 2016 或更高 | xlwings COM 交互引擎 | ✅ 必须 |
| **Python** | 3.10 或更高 | 运行时 | ✅ 必须 |
| **LibreOffice** | — | 不需要 | ❌ 不需要 |
| **gcc** | — | 不需要 | ❌ 不需要 |

> **关键优势**：Windows + xlwings 方案无需安装 LibreOffice 和 gcc，环境更简洁。

### 3.3 Python 包依赖

| 包名 | 版本 | 类型 | 用途 |
|------|------|------|------|
| **xlwings** | ≥ 0.33 | 核心 | Excel 自动化、公式重算、VBA 交互 |
| **pandas** | ≥ 2.0 | 核心 | 数据分析、统计处理 |
| **openpyxl** | ≥ 3.1 | 可选 | 仅批量数据处理加速 |
| **defusedxml** | 最新 | 可选 | 安全读取不受信任的文件 |

---

## 4. 环境搭建步骤（Windows 专项）

### 4.1 安装 Python

**方式一：官方安装包**

1. 访问 [python.org](https://www.python.org/downloads/)
2. 下载 Python 3.12.x（Windows installer 64-bit）
3. 运行安装程序，**勾选 "Add python.exe to PATH"**
4. 点击 "Install Now"

**方式二：winget 命令行**

```powershell
winget install Python.Python.3.12
```

**验证安装：**

```powershell
python --version
# 应输出：Python 3.12.x

pip --version
# 应输出：pip 2x.x from ...
```

### 4.2 创建虚拟环境

```powershell
# 进入项目目录
mkdir C:\Projects\xlsx-ai-project
cd C:\Projects\xlsx-ai-project

# 创建虚拟环境
python -m venv .venv

# 激活虚拟环境
.venv\Scripts\activate

# 验证
python -c "import sys; print(sys.prefix)"
# 应输出虚拟环境路径
```

### 4.3 安装核心包

```powershell
# 核心依赖（必须）
pip install xlwings pandas

# 可选：批量数据处理加速
pip install openpyxl

# 可选：安全读取不受信任的文件
pip install defusedxml
```

### 4.4 安装 xlwings Excel 插件

xlwings 插件提供 Excel 内的 Ribbon 工具栏和 RunPython 功能：

```powershell
# 安装 xlwings Excel 插件
xlwings addin install

# 验证：打开 Excel，应看到 "xlwings" 选项卡
```

> 如果安装失败，可能需要以管理员身份运行 PowerShell。

### 4.5 配置 Excel 信任中心

工程造价模板通常包含 VBA 宏，需要调整 Excel 安全设置：

1. 打开 Excel → 文件 → 选项 → 信任中心 → 信任中心设置
2. **宏设置** → 选择"启用所有宏"（开发阶段）
3. **受信任位置** → 添加项目目录（如 `C:\Projects\xlsx-ai-project`）
4. **外部内容** → 启用数据连接和工作簿链接
5. 点击确定，重启 Excel

### 4.6 环境验证脚本

创建 `verify_env.py` 并运行：

```python
import sys

def verify():
    print(f"Python: {sys.version}")
    checks = []

    try:
        import xlwings as xw
        checks.append(("xlwings", xw.__version__, True))
    except ImportError:
        checks.append(("xlwings", "未安装", False))

    try:
        import pandas as pd
        checks.append(("pandas", pd.__version__, True))
    except ImportError:
        checks.append(("pandas", "未安装", False))

    try:
        import openpyxl
        checks.append(("openpyxl", openpyxl.__version__, True))
    except ImportError:
        checks.append(("openpyxl", "未安装（可选）", None))

    try:
        app = xw.App(visible=False)
        app.quit()
        checks.append(("Excel COM", "连接成功", True))
    except Exception as e:
        checks.append(("Excel COM", f"失败: {e}", False))

    print("\n环境检查结果：")
    print("-" * 50)
    for name, status, ok in checks:
        icon = "✅" if ok is True else ("⚠️" if ok is None else "❌")
        print(f"  {icon} {name}: {status}")

    required_ok = all(c[2] for c in checks if c[2] is not None)
    print("-" * 50)
    print(f"整体状态: {'✅ 通过' if required_ok else '❌ 存在问题'}")

verify()
```

```powershell
python verify_env.py
```

预期输出：

```
Python: 3.12.x
环境检查结果：
--------------------------------------------------
  ✅ xlwings: 0.33.x
  ✅ pandas: 2.x.x
  ⚠️ openpyxl: 未安装（可选）
  ✅ Excel COM: 连接成功
--------------------------------------------------
整体状态: ✅ 通过
```

---

## 5. Anthropic xlsx Skill Windows 适配

### 5.1 Skill 原始架构

Anthropic 官方 xlsx Skill 的原始设计基于 Linux/macOS：

```
skills/xlsx/
├── SKILL.md              # Skill 定义和指令
├── LICENSE.txt           # 许可证
└── scripts/
    ├── recalc.py         # 公式重算（依赖 LibreOffice）
    └── office/
        └── soffice.py    # LibreOffice 沙箱兼容层
```

**原始工作流**：openpyxl 写公式 → LibreOffice headless 重算 → recalc.py 扫描错误

### 5.2 Windows + xlwings 适配方案

在 Windows 下，xlwings 直接调用 Excel 引擎，**无需 LibreOffice**：

| 原始组件 | Windows 适配 | 说明 |
|----------|-------------|------|
| `recalc.py`（LibreOffice 重算） | **xlwings 原生计算** | Excel 引擎自动重算，无需脚本 |
| `soffice.py`（沙箱兼容层） | **不需要** | Windows 无 AF_UNIX socket 限制 |
| `openpyxl`（文件操作） | **xlwings** | COM 交互替代文件操作 |
| `SKILL.md`（规范指令） | **保留核心规范** | 公式优先、零错误、颜色编码等 |

### 5.3 公式重算：xlwings 替代 recalc.py

**原始方案（Linux/macOS）**：

```bash
# 需要安装 LibreOffice
python recalc.py output.xlsx
```

**Windows + xlwings 方案**：

```python
import xlwings as xw

def recalc_with_xlwings(filepath):
    app = xw.App(visible=False)
    try:
        wb = app.books.open(filepath)
        wb.app.calculate()
        wb.save()
        wb.close()
        return {"status": "success", "total_errors": 0}
    except Exception as e:
        return {"status": "error", "message": str(e)}
    finally:
        app.quit()
```

**优势对比**：

| 对比项 | recalc.py + LibreOffice | xlwings + Excel |
|--------|------------------------|-----------------|
| 额外安装 | LibreOffice（~500MB） | 无（Excel 已装） |
| 公式兼容性 | LibreOffice 计算引擎 | Excel 原生引擎（100% 兼容） |
| 宏配置 | 需注入 Module1.xba | 无需配置 |
| 错误检测 | 需二次扫描 | Excel 实时计算即知 |
| 启动速度 | 较慢（LibreOffice headless） | 较快（COM 调用） |

### 5.4 错误检测：xlwings 方案

```python
import xlwings as xw

EXCEL_ERRORS = ["#VALUE!", "#DIV/0!", "#REF!", "#NAME?", "#NULL!", "#NUM!", "#N/A"]

def scan_errors(filepath):
    app = xw.App(visible=False)
    try:
        wb = app.books.open(filepath)
        error_details = {}
        total_errors = 0
        total_formulas = 0

        for sheet in wb.sheets:
            used = sheet.used_range
            if used is None:
                continue
            for cell in used:
                value = cell.value
                if isinstance(value, str) and value.startswith("="):
                    total_formulas += 1
                if isinstance(value, str):
                    for err in EXCEL_ERRORS:
                        if err in value:
                            loc = f"{sheet.name}!{cell.address}"
                            error_details.setdefault(err, []).append(loc)
                            total_errors += 1
                            break

        wb.close()
        return {
            "status": "success" if total_errors == 0 else "errors_found",
            "total_errors": total_errors,
            "total_formulas": total_formulas,
            "error_summary": {
                k: {"count": len(v), "locations": v[:20]}
                for k, v in error_details.items()
            }
        }
    except Exception as e:
        return {"status": "error", "message": str(e)}
    finally:
        app.quit()
```

### 5.5 SKILL.md 核心规范保留

以下规范来自 Anthropic xlsx Skill，在 Windows + xlwings 方案中继续遵循：

**公式优先原则**：

```python
# ❌ 错误：Python 计算后硬编码
total = sum(sales)
sheet.range('B10').value = total

# ✅ 正确：使用 Excel 公式
sheet.range('B10').value = '=SUM(B2:B9)'
```

**零公式错误要求**：每个交付文件必须零公式错误。

**财务模型颜色编码**：

| 颜色 | RGB | 含义 |
|------|-----|------|
| 蓝色文字 | (0,0,255) | 硬编码输入值 |
| 黑色文字 | (0,0,0) | 公式和计算 |
| 绿色文字 | (0,128,0) | 跨工作表引用 |
| 红色文字 | (255,0,0) | 外部文件链接 |
| 黄色背景 | (255,255,0) | 关键假设/待更新单元格 |

**数字格式标准**：

| 类型 | 格式 | 示例 |
|------|------|------|
| 年份 | 文本 | "2024"（非 2,024） |
| 货币 | \$#,##0 | \$1,234 |
| 百分比 | 0.0% | 15.0% |
| 倍数 | 0.0x | 8.5x |
| 负数 | 括号 | (123) 而非 -123 |
| 零值 | "-" | \$#,##0;(\$#,##0);- |

---

## 6. 项目结构参考

### 6.1 推荐目录结构

```
C:\Projects\xlsx-ai-project\
├── .venv\                          # Python 虚拟环境
├── src\
│   ├── __init__.py
│   ├── main.py                     # 程序入口
│   ├── excel_handler.py            # Excel 读写逻辑（xlwings）
│   ├── data_processor.py           # 数据处理逻辑（pandas）
│   ├── formula_validator.py        # 公式验证（xlwings 替代 recalc.py）
│   └── batch_processor.py          # 批量数据处理（openpyxl 可选）
├── templates\                      # 工程造价 Excel 模板
├── output\                         # 生成的 Excel 文件
├── tests\                          # 测试
├── requirements.txt                # 依赖清单
├── verify_env.py                   # 环境验证脚本
└── README.md                       # 项目说明
```

### 6.2 requirements.txt

```
xlwings>=0.33
pandas>=2.0
openpyxl>=3.1
defusedxml>=0.7
```

### 6.3 xlwings 基础用法速查

```python
import xlwings as xw

# 创建新工作簿
app = xw.App(visible=False)
wb = app.books.add()
sheet = wb.sheets[0]

# 写入数据
sheet.range('A1').value = '项目名称'
sheet.range('B1').value = '合价（万元）'

# 写入公式
sheet.range('B10').value = '=SUM(B2:B9)'

# 设置格式
sheet.range('A1').api.Font.Bold = True
sheet.range('A1').api.Font.Color = 0x0000FF  # 蓝色
sheet.range('B2:B9').api.NumberFormat = '#,##0.00'

# 保存
wb.save('output/工程造价汇总.xlsx')
wb.close()
app.quit()

# 读取现有文件
app = xw.App(visible=False)
wb = app.books.open('templates/工程量清单.xlsx')
sheet = wb.sheets['分部分项工程费']

# 读取数据
data = sheet.range('A1:D20').value  # 返回二维列表

# 公式自动计算——无需 recalc.py
wb.app.calculate()

# 保存并关闭
wb.save()
wb.close()
app.quit()
```

---

## 7. 常见问题与排障

### 7.1 xlwings COM 错误

**问题**：`com_error: (-2147221005, '无效的类字符串', None, None)`

**原因**：Excel 未正确安装或注册。

**解决**：

```powershell
# 修复 Excel 注册
# 控制面板 → 程序和功能 → Microsoft Office → 更改 → 联机修复

# 或重新安装 xlwings 插件
xlwings addin install
```

**问题**：`PermissionError: [WinError 32]` 文件被占用

**原因**：Excel 进程未正常退出。

**解决**：

```powershell
# 强制关闭所有 Excel 进程
taskkill /F /IM EXCEL.EXE
```

### 7.2 Excel 进程残留

xlwings 操作异常中断后，Excel 进程可能残留。建议在代码中使用 try/finally：

```python
import xlwings as xw

app = xw.App(visible=False)
try:
    wb = app.books.open('file.xlsx')
    # ... 操作 ...
    wb.save()
    wb.close()
finally:
    app.quit()  # 确保 Excel 进程退出
```

也可使用上下文管理器模式：

```python
with xw.App(visible=False) as app:
    wb = app.books.open('file.xlsx')
    # ... 操作 ...
    wb.save()
```

### 7.3 openpyxl 批量处理集成

当需要处理万行级以上纯数据时，可切换到 openpyxl：

```python
import openpyxl
import pandas as pd

def batch_read_large_file(filepath, sheet_name=None):
    df = pd.read_excel(
        filepath,
        sheet_name=sheet_name or 0,
        engine='openpyxl',
        dtype={'编号': str}
    )
    return df

def batch_write_large_file(df, filepath):
    df.to_excel(filepath, index=False, engine='openpyxl')
```

**注意**：openpyxl 写入的公式不会被计算。如需公式计算，后续需用 xlwings 打开重算：

```python
import xlwings as xw

app = xw.App(visible=False)
wb = app.books.open(filepath)
wb.app.calculate()
wb.save()
wb.close()
app.quit()
```

### 7.4 xlwings 性能优化

```python
# 关闭屏幕刷新和自动计算，大幅提升写入速度
app = xw.App(visible=False)
app.calculation = 'manual'       # 关闭自动计算
app.screen_updating = False      # 关闭屏幕刷新

try:
    wb = app.books.add()
    sheet = wb.sheets[0]
    # ... 批量写入操作 ...
    app.calculate()               # 最后统一计算
    wb.save('output.xlsx')
    wb.close()
finally:
    app.screen_updating = True
    app.calculation = 'automatic'
    app.quit()
```

### 7.5 .xls 旧格式处理

工程造价领域可能遇到 .xls 格式的旧模板：

```python
import xlwings as xw

# xlwings 直接支持 .xls 格式
app = xw.App(visible=False)
wb = app.books.open('old_template.xls')  # 直接打开
# ... 操作 ...
wb.save('new_template.xlsx')              # 另存为 .xlsx
wb.close()
app.quit()
```

---

## 附录 A：完整安装命令速查

```powershell
# 1. 创建项目
mkdir C:\Projects\xlsx-ai-project
cd C:\Projects\xlsx-ai-project

# 2. 创建并激活虚拟环境
python -m venv .venv
.venv\Scripts\activate

# 3. 安装核心依赖
pip install xlwings pandas

# 4. 安装可选依赖（批量数据处理）
pip install openpyxl defusedxml

# 5. 安装 xlwings Excel 插件
xlwings addin install

# 6. 验证环境
python verify_env.py
```

## 附录 B：与 Anthropic xlsx Skill 的对照表

| Skill 原始组件 | Windows 适配 | 替代方案 |
|----------------|-------------|---------|
| `openpyxl` 读写 | `xlwings` COM 交互 | Excel 原生操作 |
| `pandas` 数据分析 | `pandas`（不变） | — |
| `recalc.py` + LibreOffice | `xlwings` Excel 引擎 | `app.calculate()` |
| `soffice.py` 沙箱兼容 | 不需要 | Windows 无 AF_UNIX 限制 |
| `SKILL.md` 规范 | 保留核心规范 | 公式优先、零错误、颜色编码 |
| `Module1.xba` 宏注入 | 不需要 | xlwings 原生支持 VBA |
| 错误检测（7 种 Excel 错误） | xlwings 扫描实现 | 逻辑一致，引擎不同 |
